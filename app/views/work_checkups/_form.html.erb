<%= form_for work_checkup do |form| %>
  <% if work_checkup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(work_checkup.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
        <% work_checkup.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :private_number %>
    <%= form.number_field :private_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
